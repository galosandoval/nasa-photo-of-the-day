import React from 'react';

const NavBar = (props) => {
  return (
    <nav>
      <a href="#">Home</a>
      <a href="#">Contact</a>
      <a href="#">About</a>
      <a href="#">More Pictures</a>
    </nav>
  )
}

export default NavBar